#ifndef MAIN_H
#define MAIN_H
#include <limits.h>
#include <stdio.h>
#include <stdarg.h>
#include <unistd.h>
int _putchar(char c);
int _puts(char *str);
int _printf(const char *format, ...);

#endif /* MAIN_H */
